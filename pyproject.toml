[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
requires-python = ">=3.12"
name = "filekeep"
version = "0.0.0"
authors = [{ name = "Gonçalo MB", email = "me@goncalomb.com" }]

[project.scripts]
filekeep = "filekeep:main"
